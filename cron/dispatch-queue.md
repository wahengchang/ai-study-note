# Research & Writing Dispatch Queue

> Generated by the `cron/git-auto.md` triage pass. Captures the filtered, agent-routed view of the open issue queue so the next `git-auto` run can pick up deterministically.
>
> **Run ID**: `iNJ4i` · **Audit date**: 2026-04-14 · **Base**: `origin/main`

## Scope

Filter applied to open issues: **research** or **writing** intent (labels `documentation` / `enhancement`, body describes a `study note`, `research note`, or `learn note` deliverable). Bug-fix, infra, and framework-adoption issues are excluded from this queue.

## Eligibility rules (from `cron/git-auto.md`)

- One issue per branch, one branch per PR.
- Skip issues with an existing open PR or `in_progress` status.
- Pick the lowest-numbered eligible issue per run.
- Branch fresh from `origin/main` as `auto/issue-<n>`.

## Audit result

| # | Title | Intent | Agent | Open PR | Eligible |
|---|---|---|---|---|---|
| 61 | 寶藏開源項目，自動優化專業級提示詞 (research) | Research | `@writer` | — | yes |
| 63 | Adopt Mintlify as new framework | Infra eval | `@content-ops` | #124 | no (out of scope + PR open) |
| 67 | Fix duplicate article titles | Bug / content fix | `@content-ops` | — | no (out of scope) |
| 74 | Study note on Astron Agent / Serper / Jina / Python node / LLM | Writing | `@writer` | #121 | no (PR open) |
| 75 | Learn note on LLM raw/wiki knowledge management | Research | `@writer` | #120 | no (PR open) |
| 76 | Research n8n AI YouTube automation workflow | Research | `@writer` | — | yes |
| 77 | Research terminal multiplexing (cmux) workflow for Claude Code | Research | `@writer` | — | yes |

## Delegation (eligible only)

### #61 — `@writer`

- **Deliverable**: research note confirming the project referenced in the 小紅書 post, with TL;DR, core features, and repo link.
- **Operational requirements**:
  - Resolve the 小紅書 short link to the actual project / repo.
  - Produce TL;DR (3–5 lines), core feature list, ≥3 reusable takeaways, and an inclusion recommendation.
  - Target folder: `content/research-notes/` (pending `@content-ops` confirmation against `docs/content-taxonomy.md`).
  - Language: zh-tw.

### #76 — `@writer`

- **Deliverable**: end-to-end breakdown of the n8n AI-YouTube workflow plus a tool catalogue and a minimal PoC plan.
- **Operational requirements**:
  - Decompose the workflow into stages (ideation → script → visual → audio → assembly → publish → ops).
  - Categorise tools (orchestration / LLM / visual / audio / rendering / publishing / storage) with usage, limits, cost.
  - Deliver a PoC architecture sketch (Mermaid `flowchart LR`) and explicit risks (quality, copyright, YouTube policy, zh-tw viability).
  - Target folder: `content/research-notes/`. Language: zh-tw.

### #77 — `@writer`

- **Deliverable**: research note clarifying `cmux` identity and positioning terminal-multiplexing workflows that amplify Claude Code.
- **Operational requirements**:
  - Identify the concrete project referenced (`cmux` vs. `tmux` / `zellij` / WezTerm mux / Warp).
  - Compare ≥3 multiplexer/session tools against Claude Code usage patterns (agent pane, logs pane, git/test pane, long-running monitor).
  - Produce one adoptable minimal workflow with applicability and constraints.
  - Target folder: `content/research-notes/`. Language: zh-tw.

## Next execution pick

- **Issue**: #61
- **Branch**: `auto/issue-61` (branch fresh from `origin/main`).
- **Agent**: `@writer`
- **Subsequent picks** (later runs, after #61 closes): #76, then #77.

## Out-of-scope this run

- #63, #67 — not research/writing intent.
- #74, #75 — already represented by open PRs (#121, #120).

PRs already in flight stay in the `@reviewer` lane for comment/review follow-up, per `cron/git-auto.md` §3.
