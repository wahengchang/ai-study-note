# PR #361／#364 審查與修正

- **完成時間**：2026-09-12T10:30+08:00
- **狀態**：completed（審查交付；部分發現需 Owner 決策）

## 交付

- 審查 PR #361（`cms:init`／`cms:start`）與 PR #364（本機瀏覽器直接使用 CMS）。
- 修正 `cms-kill-cli.ts` 的 module-load side effect：加上 entrypoint guard、抽出
  `runCmsKillCli`、限定 loopback listener、拒絕多餘參數、`process.kill` 容錯。
- 新增 `tests/apps/cli/cms-kill-cli.test.ts`（3 cases）。
- CMS document shell 由 `CMS 工作台已鎖定` 改為載入中 shell（#364 已移除 lock 概念）。
- 更新 `.rulesync/skills/demo-e2e/SKILL.md` 並重新產生 generated view；一併修正
  branch 上既有的 `check:ai-sync` 落後。
- 更新 `contracts/README.md` 的 `cms:kill` 行以符合實作。

## 關鍵決策

未自行修正 #364 的 authentication 移除。該變更由 Owner 在 contract 中明示
（「deliberately grants any process on the same machine access」），屬範圍決策，
不由審查單方面回退。改以審查意見交回 Owner。

## 實際驗證

- `npm run typecheck`、`npm run check:architecture`、`npm run check:ai-sync`：通過。
- 非 browser 測試 257 項：254 通過、3 失敗。3 項失敗全部源自 #364，與本次修正無關：
  - `CMS documents and manifest assets apply their independent Fetch Metadata gate`
    （`Origin: https://attacker.example` 的 asset 請求 200，contract 要求 403）
  - `publish rejections stay outside the command seam and canonical state`
    （無 credential 的 publish 得到 409，代表已抵達 command seam，contract 要求 401）
  - `every rejected transport shape fails closed with its contract status and mutates nothing`
    （無 credential 的 read 得到 200，contract 要求 401）
- 同一檔案在 #361 head 為 19/19 通過，確認 3 項失敗由 #364 引入。
- 新增與既有 CLI 測試 5/5 通過。

## 已知限制／後續

- 本環境無法執行 5 個 Playwright 測試：專案 pin `playwright@1.63.0`，容器只有
  `chromium-1194`，且 browser 下載被 proxy 阻擋。`article-workspace.test.ts` 等會
  無輸出地 hang（launch 失敗未被視為失敗），因此 `npm test` 整體無法在此完成。
  這也解釋 #364 的 PR body 為何只列 typecheck／cms:build／check:architecture。
- 待 Owner 決策：`/_local/browser-tickets` 與 `/_local/browser-session` 在 #364 後
  仍然存在並仍會交出 durable `asn_v1_` API key，但 contract 已刪除其描述。

## 相關 Branch／PR

- Branch：`fix/cms-local-review`（base `feat/local-browser-cms`）
