# Theme Host

- **Cycle**：`cycle-2026-08-29-1002-cms-issue-backlog`
- **完成時間**：2026-09-04T10:50:13+08:00
- **狀態**：completed（非 Cycle final Work Group）
- **Branch／PR**：`cms/theme-host`；PR 尚未建立（`null`）

## 交付

- 建立 `core/theme-host/index.ts` 的唯一 public seam：external installed root 的 Theme discovery、exact identity resolution 與 verified defensive file read。
- 固定 canonical `theme.json`、identity/evidence、descriptor revalidation、self-contained runtime scan 與 structured failure routing；Theme bytes 永不執行。
- ThemeHost external dependency allowlist、extension bare/import-type edge routing 與架構 fixture 已同步實作。
- `docs/INDEX.md` 已連到實作與 Theme Host contract test。

## 關鍵決策

- opaque slot 不參與 identity；同 `{id,version}` 先盤點全部可辨識 manifest identity，再 fail closed，不讓 evidence drift 的 duplicate 放行另一 slot。
- 載入以 `O_NOFOLLOW` descriptor 綁定，並限制 256 slots、128 resources、1 MiB manifest、16 MiB evidence file、64 MiB package，避免 trusted-current-UID 路徑仍可造成無界讀取。
- 本 Work Group 只驗證 runtime import graph，不 `import()`、不啟用 Theme，也不定義 Renderer callback。

## 實際驗證

- `node --import tsx --test tests/core/theme-host/theme-host.test.ts tests/core/foundation/check-architecture.test.ts`：28/28 通過。
- `npm run typecheck && npm run check:architecture`：通過。
- `npm run check`：175/175 通過。
- 實際 runtime 為 Node `v22.22.0`／npm `10.9.4`；與 contract 指定 Node `24.20.0`／npm `11.19.0` 不一致，因此未宣稱指定 runtime 版本已通過。

## 已知限制／後續

- Theme runtime 只做 verified-byte import graph validation；沒有 runtime execution、sandbox、activation、Renderer input/output、Plugin hooks 或 CMS UI。
- WI-028、WI-030、WI-029、WI-031、WI-040 保持未認領；Theme Host PR 合併後才依獨立計畫核對 WI-028。
