# 開發宣言寫入 MEMORY

## 交付

- 將專案長期有效的 architect、developer 與 working-principle 準則寫入 `MEMORY.md`。
- 宣言明確定義真理、原則、規則、決策檢驗與已核准工程邊界。

## 關鍵決策

- 以「知識治理」而非 CMS-first、API-first 或 static-site-first 作為架構主軸。
- 將唯一 canonical state、immutable history、explicit published projection、deterministic delivery、owned boundaries 與 fail-closed 視為跨技術世代不變的準則。
- 保留 `CMS-BASIC-CONTRACTS-V1` 為精確實作邊界；`MEMORY.md` 只記錄其長期可用的摘要與指標。

## 驗證

- 已寫入並取得 `MEMORY.md` 成功回應：3,849 bytes。

## 已知限制／後續

- 未來若已核准契約改變，更新 `已核准工程邊界`；除非改變上述跨技術世代的方向，否則不改動真理、原則與規則。
