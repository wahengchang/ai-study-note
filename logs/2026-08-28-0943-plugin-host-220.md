# Plugin Host trusted activation

- Cycle：`cycle-2026-08-28-0929-plugin-host-220`
- 完成時間：2026-08-28T09:43:16+08:00
- 狀態：completed

## 交付

- 建立 `core/plugin-host/` public contract、固定去敏 failure、trusted repository-external root 驗證與 canonical `plugin-manifest/v1` discovery。
- activation 在 executable/resource digest 與 callback export 驗證後，透過 injected durable CAS port 保存 exact identity；提供 explicit deactivation 與 revalidated atomic active snapshot。
- 加入 self-contained `activation-probe` source template 與 OS-temp public seam contract tests。
- 建立後續 integration issue #246，供 #220、#228、#229、#234 完成後由 Application composition 以真實 adapter 組裝。

## 關鍵決策

- Production 只定義必填 `PluginActivationStatePort`，不引入 Persistence dependency、in-memory/file fallback 或 production mock。
- Plugin source template 永不作為 installed runtime root；dynamic import 只接受 manifest-hash-bound、已完成 containment 與 evidence 驗證的 entry URL。
- trusted-local 是 capability boundary，不宣稱限制已載入 trusted Plugin 的 host-external filesystem/network 行為。

## 實際驗證

- Node `v24.20.0`、npm `11.19.0`。
- `node --import tsx --test "tests/core/plugin-host/*.test.ts"`：6/6 通過。
- `npm run check`：typecheck、architecture 與 49 tests 通過。
- `npm run check:ai-sync`：通過。

## 已知限制／後續

- #220 只驗證 callback exports；不執行 SaveRevision hooks。
- #246 維持 non-ready，等待 #220、#228、#229、#234 後才實作 Application composition。

## 相關 Branch／PR

- Branch：`cms/plugin-host-220`
- PR：https://github.com/wahengchang/ai-study-note/pull/247
