# ChangeRoute 工作紀錄

- **完成時間**：2026-09-03T14:02:02+08:00
- **Work Item**：WI-023／GitHub #238
- **Work Group**：WG-014
- **狀態**：completed

## 交付

- `DomainApplication` 新增 transaction-bound `changeRoute` command、request/success public types 與固定 failure codes。
- 只以 SiteDefinition issued proposal 在 configured transaction 內驗證與套用；pointer 維持 selected revision，寫入 `ChangeRoute` non-revision lineage。
- 新增 route replacement、pointer/lineage rollback、競爭、雙圖隔離、request fail-closed 與 error redaction 的 observable test。
- `docs/INDEX.md` 已連至 ChangeRoute 現況與測試。

## 關鍵決策

- token 驗證後不再讀取 caller-owned proposal；先套用 token，再僅使用 `applied.value.claim` 執行 pointer gate 與 lineage，避免 stateful accessor 把 lineage 導向其他 entry。
- ChangeRoute 的 schema、media 與 revision/reference applicable sets 為空；command 不讀取或重新驗證它們。
- SiteDefinition transaction snapshot storage failure 與 stale digest/token failure 分離；前者由 Application 收斂為 `CHANGE_ROUTE_FAILED`。

## 實際驗證

- `node --import tsx --test tests/core/application/change-route.test.ts`：4/4 通過。
- `node --import tsx --test tests/core/application/save-revision.test.ts tests/core/application/save-revision-failures.test.ts tests/core/application/publish-revision.test.ts tests/core/application/restore-revision.test.ts tests/core/site-definition/route-claim-replacement.test.ts`：22/22 通過。
- `npm run typecheck && npm run check:architecture`：通過。
- `npm run check`：166/166 通過。

## 已知限制／後續

- 本機實際 runtime 為 Node v22.22.0／npm 10.9.4；contract 指定 Node 24.20.0／npm 11.19.0。上述驗證未因 mismatch 省略。
- WI-028、WI-029 尚未完成；API-06 仍不得建立 Work Group。

## Branch／PR

- branch：`cms/change-route`
- PR：待建立；URL 以 WG-014 的 tracking commit 記錄。
